node_modules/
dist/
*.tgz
# src/shims/ — kept in repo, required for standalone compilation

# Local secrets (never commit)
.env
.env.*
local-config.*
.mcp.json

# Closed-source WCPP MAX binaries
wechatpadpromax*/

# Offline API docs mirror (closed-source, not redistributable)
docs/api-reference/
