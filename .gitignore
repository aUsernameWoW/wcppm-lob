node_modules/
dist/
*.tgz
src/shims/

# Local secrets (never commit)
.env
.env.*
local-config.*
.mcp.json

# Closed-source WCPP MAX binaries
wechatpadpromax*/

# Offline API docs mirror (closed-source, not redistributable)
docs/api-reference/
